name: CI

on:
  push:
  pull_request:

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Syntax check
        run: bash -n install-agentic-tools.sh
      - name: ShellCheck
        run: shellcheck install-agentic-tools.sh
